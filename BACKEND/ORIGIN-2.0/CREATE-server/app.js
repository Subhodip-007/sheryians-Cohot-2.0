const express = require('express')
const app = express() // server is created but cant be accessed(start)
// when u call express it creates a inscens and save in valiable 
app.listen(3000) // server ko start krta hai
 // till no we have created and start server 