//ROUTING
// now in a webpage their are multiple pages 
// like sheriyans.com
//  i want to go to course page so it s route is sheriyans.com/courses
going from one page to other 
//  now react own dont have routing so we use a third party library (react router DOM)

now main main 
react - UI banana
react-DOM- react or DOM in btw connection
to instrall 
npm i react-router-dow is a tool to perform routing in react
react router DOM doc 
there r many things but for now lets understand
- instrallation 
- how to perform a basic routing inside application 
- to perform we have to frest wrap out whole application  wite browser router 

--  go  to main.jsx - import browser router 
and wrap <BrowserRouter> <app  /> </BrowserRouter>
what is browser router 
-- type of router that uesr history api in easy way it remember your prev and new tab back
now in app.jsx create router of pagss now in roution there is now redirection it acts as a single page application
only the part ur changing wil chance the whole websiet wont reload eg search in youtube 
now to make it make a nav bar comp outside routs and add a tag to redirect pages u will see relooad to solve use link tags 
now lets understand nested routes
// dynamic route
// if you try to search about/anything that dont exist that a dynamic page opeanes in every result
// nested dynamic route
// not found page 